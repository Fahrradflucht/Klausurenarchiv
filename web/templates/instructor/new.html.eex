<h2>New instructor</h2>

<%= render "form.html", changeset: @changeset,
                        action: course_instructor_path(@conn, :create, @course) %>

<%= link "Back", to: course_instructor_path(@conn, :index, @course) %>
