<h2>Neue Klausur</h2>

<%= render "form.html", changeset: @changeset,
                        action: course_instructor_upload_path(@conn, :create, @course, @instructor) %>

<%= link "Zurück", to: course_instructor_upload_path(@conn, :index, @course, @instructor) %>
