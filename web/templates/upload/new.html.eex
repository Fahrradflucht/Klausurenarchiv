<h2>New upload</h2>

<%= render "form.html", changeset: @changeset,
                        action: course_instructor_upload_path(@conn, :create, @course, @instructor) %>

<%= link "Back", to: course_instructor_upload_path(@conn, :index, @course, @instructor) %>
